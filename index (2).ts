import { CheckCircle2, Circle, Store } from 'lucide-react';
const steps = ['Business profile','Programme type','Locations','Reward rules','Branding','Funding','Staff','Test transaction','Review and launch'];
export function MerchantOnboarding(){return <main className="min-h-screen bg-slate-950 px-4 py-10 text-white"><div className="mx-auto max-w-4xl"><p className="text-sm font-semibold text-violet-300">Merchant onboarding</p><h1 className="mt-2 text-4xl font-bold">Launch a loyalty programme without banking.</h1><p className="mt-3 text-white/60">The same tenant can later connect Zoryn Money, ZorynPay, POS, affiliate and card-linked channels.</p><div className="mt-8 rounded-3xl border border-white/10 bg-white/[0.04] p-6"><div className="flex items-center gap-3"><Store className="text-violet-300"/><div><h2 className="font-semibold">Café Berlin Rewards</h2><p className="text-sm text-white/50">Growth plan • 3 locations</p></div></div><div className="mt-6 space-y-3">{steps.map((s,i)=><div key={s} className="flex items-center gap-3 rounded-xl bg-white/[0.04] p-4">{i<5?<CheckCircle2 className="text-emerald-400"/>:<Circle className="text-white/30"/>}<div className="flex-1"><p className="font-medium">{s}</p><p className="text-xs text-white/45">{i<5?'Complete':i===5?'Action required':'Not started'}</p></div><button className="rounded-lg bg-white/10 px-3 py-2 text-sm">{i<5?'Review':'Continue'}</button></div>)}</div></div></div></main>}
