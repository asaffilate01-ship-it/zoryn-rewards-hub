import { CalendarDays, Target, Users, WalletCards } from 'lucide-react';
const stages=[['Goal','Increase repeat visits',Target],['Audience','Customers inactive 30–90 days',Users],['Timing','Monday–Thursday, 14:00–17:00',CalendarDays],['Reward','3× points, max 600 points',WalletCards]];
export function CampaignStudio(){return <main className="min-h-screen bg-[#080b1a] px-4 py-10 text-white"><div className="mx-auto max-w-5xl"><p className="text-sm font-semibold text-violet-300">Campaign Studio</p><h1 className="mt-2 text-4xl font-bold">Build measurable loyalty campaigns.</h1><div className="mt-8 grid gap-4 md:grid-cols-2">{stages.map(([label,value,Icon])=><section key={String(label)} className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><Icon className="text-violet-300"/><p className="mt-5 text-sm text-white/50">{String(label)}</p><p className="mt-1 font-semibold">{String(value)}</p><button className="mt-4 rounded-lg bg-white/10 px-3 py-2 text-sm">Edit</button></section>)}</div><section className="mt-5 rounded-2xl border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-semibold">Budget and forecast</h2><div className="mt-4 grid gap-4 md:grid-cols-3"><div><p className="text-sm text-white/50">Reward budget</p><p className="text-2xl font-semibold">€900</p></div><div><p className="text-sm text-white/50">Estimated reach</p><p className="text-2xl font-semibold">1,240</p></div><div><p className="text-sm text-white/50">Forecast revenue</p><p className="text-2xl font-semibold">€6,400</p></div></div><button className="mt-6 rounded-xl bg-violet-600 px-5 py-3 font-semibold">Submit for approval</button></section></div></main>}
